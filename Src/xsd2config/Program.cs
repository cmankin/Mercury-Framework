using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace xsd2config
{
    class Program
    {
        private const string LONG_NAME_WITH_EXE = "xsd2config.exe";
        private const string LONG_NAME = "xsd2config";
        private const string SHORT_NAME = "xsd2cfg";

        static void Main(string[] args)
        {
            if (args != null && args.Length > 0)
            {

            }
            else
            {

            }
        }

        private static void __EnterMainLoop()
        {
        }
    }
}
